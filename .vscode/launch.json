{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with GDB",
            "type": "gdb",
            "request": "launch",
            "program": "${workspaceFolder}/${fileBasenameNoExtension}/exe",
            "cwd": "${workspaceFolder}"
        }
    ]
}